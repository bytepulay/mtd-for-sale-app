<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asia.nainglintun.myintthitar">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2" /> <!-- <uses-feature android:name="android.hardware.Camera"/> -->
    <!-- <uses-feature android:name="android.hardware.Camera.autofocus"/> -->
    <uses-feature android:name="android.hardware.camera2.autofocus" />

    <application
        android:name=".activities.Activities.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/mtdicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Activities.ScanForVoucherActivity"></activity>
        <activity android:name=".activities.Activities.LoginTestActivity" />
        <activity
            android:name=".activities.Activities.VoucherDetailActivity"
            android:label="Sale Voucher Detail" />
        <activity
            android:name=".activities.Activities.VoucherOrderDetailActivity"
            android:label="Order Voucher Detail" />
        <activity
            android:name=".activities.Activities.CustomerActivity"
            android:label="@string/title_activity_customer" />
        <activity
            android:name=".activities.Activities.SalesActivity"
            android:label="@string/title_activity_sales" />
        <activity android:name=".activities.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>