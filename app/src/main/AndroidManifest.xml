<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asia.nainglintun.myintthitar">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2" /> <!-- <uses-feature android:name="android.hardware.Camera"/> -->
    <!-- <uses-feature android:name="android.hardware.Camera.autofocus"/> -->
    <uses-feature android:name="android.hardware.camera2.autofocus" />

    <application
        android:name=".Activities.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/mtdicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Comfirm"></activity>
        <activity android:name=".Activities.ScanForVoucherActivity" />
        <activity
            android:name=".Activities.CustomerActivity"
            android:label="@string/title_activity_customer" />
        <activity
            android:name=".Activities.SalesActivity"
            android:label="@string/title_activity_sales" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginTestActivity" />
        <activity android:name=".Activities.OrderScanForVoucherActivity" />
    </application>

</manifest>